export default async function run(page, ui) {
  // ??? ?????? ????? inline? ????????? const ?? ????? ??? window. ????? ?? appState ???? ?? const ?? var
  const r = await page.evaluate(async () => {
    const txt = await (await fetch('js/supabase.js')).text();
    const idx = txt.indexOf('appState');
    return { aroundAppState: idx >= 0 ? txt.slice(Math.max(0,idx-80), idx+250) : 'appState not found in supabase.js' };
  });
  return r;
}
