export default async function run(page, ui) {
  // ??? 404 ?? ?? admin scripts. ??? ??? supabase.js ?? ????? ???? ???? context ?????
  // ??? ????: ?????? ????? ?????? ???? ????. ???? evaluate ???? isolated world
  const r = await page.evaluate(() => {
    // ???? ????? ?? supabase.js - ?? showToast
    const probes = {
      showToast: typeof window.showToast,
      loadProducts: typeof window.loadProducts,
      updateCartBadge: typeof window.updateCartBadgeFromDB,
      appStateLexical: (function(){ try { return typeof appState; } catch(e){ return 'err'; } })()
    };
    return probes;
  });
  return r;
}
