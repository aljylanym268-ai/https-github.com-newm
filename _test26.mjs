export default async function run(page, ui) {
  // UMD ?????? ?? ?? ????? window.supabase. ?? ?? CSP ?? ?? ??????? headless ??????
  // ???? ????? ???? ???? ?? ??? cache
  const r = await page.evaluate(async () => {
    const resp = await fetch('https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/dist/umd/supabase.js');
    const txt = await resp.text();
    return { len: txt.length, head: txt.slice(0, 200) };
  });
  return r;
}
